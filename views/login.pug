extends ./layout/index

block content 
    link(rel="stylesheet", href="/css/signin.css")
    form(method="post" action="/user/login").form-signin
        img.mb-4(src='https://getbootstrap.com/docs/4.0/assets/brand/bootstrap-solid.svg' alt='' width='72' height='72')
        h1.h3.mb-3.font-weight-normal Please Log in
        label.sr-only(for='inputEmail') Email address
        input#inputEmail.form-control.mb-2(type='text' placeholder='Username' required='' autofocus='' name='username' value= (info.username ? info.username : null))
        if errors.user
            div(class="alert alert-danger") #{errors.user}
        label.sr-only(for='inputPassword') Password
        input#inputPassword.form-control(type='password' placeholder='Password' required='' name='password')
        if errors.password
            div(class="alert alert-danger") #{errors.password}
        //- .checkbox.mb-3
            //
                <label>
                <input type="checkbox" value="remember-me"> Remember me
                </label>
        button.btn.btn-lg.btn-primary.btn-block(type='submit') Log in
        a(href="/user/forgot" id="recover").text-muted.float-right.mt-3 Recover your password 
        br
        p.mt-5.mb-3.text-muted © 2017-2018
    a(href="/user/register")
        button.btn.btn-primary.btn-sm Register
    a(href="/user/login/github")
        button.btn.btn-primary.btn-sm Login with github